module.exports = {
    mode: "jit", // Ensure JIT mode is enabled
    content: ["./*.html", "./src/**/*.{js,jsx,ts,tsx}"], // Make sure all necessary files are included
    theme: {
      extend: {},
    },
    plugins: [],
  };
  